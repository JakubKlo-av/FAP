Return only the JSON. No explanations, no markdown.

OCR extract of an XPD Global / Europartners Mexico invoice.
Two pages with the same content - extract from Page 1, use Page 2 only as fallback.
Do not infer, calculate, or paraphrase - extract exactly what appears in the OCR.

Fields to extract:

1. vendor - always: "Europartners Mexico"
2. invoice_number - upper-right, row with "Serie y folio interno"; starts with "FCUS-" (e.g. "FCUS-620207"). Extract ALL digits.
3. bill_to - always: "Autoliv ASP, Inc. 3350 Airport Road Ogden, UT 84405 USA"
4. invoice_date - right area, line with "Fecha/Date:"; extract date to the right. Convert to DD/MM/YYYY.
5. ship_date - line with "ETD/ETA:"; two dates on the line - take the FIRST (ETD). Convert to DD/MM/YYYY.
6. shipper - lines below "Embarcador/Shipper:" until "Consignatario/Consignee:" (OCR: "Consianatario/Consianee:"); join with ", ".
7. consignee - lines below "Consignatario/Consignee:" until "Linea/Line:"; join with ", ".

8. tms_number, ptn_number, autoliv_person_concerned
   Lines under "Fact. Comercial:/Comercial INV:" (left side) until "Corresponsal:/Agent:".
   Join all lines with " ", then split by " // ".
   OCR correction: leading '$' in codes -> 'S' (e.g. "$92640" -> "S92640").

   PATTERN A (any fragment = "TMS" or starts with "tms"):
     -> tms_number = "load <number>", ptn_number = "N/A", autoliv_person_concerned = two-word name after TMS identifier

   PATTERN B (any fragment starts with "PTN:"):
     -> tms_number = "N/A", ptn_number = value after "PTN:" ($ -> S), autoliv_person_concerned = two-word name immediately after PTN code

   Person name = always first name + last name only (no company/location). If neither pattern -> all three = "N/A".

9. weight_value - line with "Peso Bruto:/Gross weigth"; numeric value before the unit.
10. weight_unit - unit on the same line as weight_value (e.g. "KG").
11. currency - 3-letter code from the first data row below the "Impuestos" header.
12. line_items - rows below "Impuestos" header until the line starting with "(":
    columns: code | description | quantity | unit_price | tax_type | currency | amount
    Strip commas from amounts. Return as JSON array ([] if none).
13. amount - line with "TOTAL :"; numeric value to the right, strip commas. Only TOTAL line (not PARCIAL, not I.V.A.).
14. mawb - line immediately below "B/L:MAWB:"; if "N/A" or blank -> "N/A".
15. hawb - line immediately below "FBL:/HAW:" (same header line as B/L:MAWB:); if blank -> "N/A".
16. bultos - numeric value from the data line below the "Bultos:/PCS" column header.

=== RULES ===
- Missing field -> "N/A" ([] for line_items).
- Extract only what is literally present in the OCR.
- Return a single JSON array with one object:

[
  {{
    "vendor": ...,
    "invoice_number": ...,
    "bill_to": ...,
    "invoice_date": ...,
    "ship_date": ...,
    "shipper": ...,
    "consignee": ...,
    "tms_number": ...,
    "ptn_number": ...,
    "autoliv_person_concerned": ...,
    "weight_value": ...,
    "weight_unit": ...,
    "currency": ...,
    "line_items": [...],
    "amount": ...,
    "mawb": ...,
    "hawb": ...,
    "bultos": ...
  }}
]

Document text:
"""
{extracted_text}
"""
