Return only the JSON. No explanations, no markdown.

OCR extract of a Courier Network Inc. invoice.
Document has THREE columns — LEFT: shipment details, CENTER: shipper/consignee, RIGHT: references.
Do not infer or calculate. OCR correction: '$' in non-numeric context → 'S'.

Fields to extract:

1. supplier — always: "Courier Network Inc."
2. invoice_no — alphanumeric code immediately after "Tax Invoice" (ignore trailing words like "Original").
3. invoice_date — after "Invoice Date:"; convert to DD/MM/YYYY.
4. bill_to — always: "Autoliv ASP, Inc. 3350 Airport Road Ogden, UT 84405 USA"
5. awb — value after "AWB #:" or "IAWB#:" (LEFT column).
6. ship_date — date after "Pickup Date:" (LEFT column); convert to DD/MM/YYYY.
7. delivery_date — date part after "Pod:" (LEFT column); convert to DD/MM/YYYY.
8. delivery_time — time part on the same "Pod:" line, after the date.
9. delivery_timezone — always "N/A".
10. shipper — lines below "SHIPPER:" (CENTER) until "CONSIGNEE:"; join with ", ".
    Include ALL lines: company name, street, city/zip, and country code — do NOT omit the last line.
11. consignee — lines below "CONSIGNEE:" (OCR may read "CONSIGNEE!") (CENTER) until billing table header ("Service Description"); join with ", ". Include country code.
12. mawb — value after "MAWB:" (RIGHT column); '$' → 'S'. If absent → "N/A".
13. ptn — value after "PTN:" (RIGHT column); '$' → 'S'.
14. reqna — value after "REQNA:" (RIGHT column).
15. tms_number — value after "TMS Number:" (bottom of document, after billing table).
16. weight_value — numeric part after "WEIGHT:" or "EIGHT:" (LEFT column; OCR may drop the leading "W").
17. weight_unit — unit on the same line as weight_value.
18. pieces — numeric value after "PIECES:".
19. line_items — rows after header "Service Description | Amount | Currency | Exchange Rate | Amount Inv Currency",
    until the line containing "TOTAL":
    for each row: description, amount (no commas), currency, exchange_rate, amount_inv_currency (no commas).
    Return as JSON array ([] if none).
20. invoice_total — numeric value on "Total Amount Due" line; strip commas.
21. invoice_currency — 3-letter code on the "Total Amount Due" line (before the amount).

=== RULES ===
- Missing field → "N/A" ([] for line_items).
- Never copy a value from another field if source is blank.
- Extract only what is literally present in the OCR.
- Return a single JSON array with one object:

[
  {{
    "supplier": ...,
    "invoice_no": ...,
    "invoice_date": ...,
    "bill_to": ...,
    "awb": ...,
    "ship_date": ...,
    "delivery_date": ...,
    "delivery_time": ...,
    "delivery_timezone": ...,
    "shipper": ...,
    "consignee": ...,
    "ptn": ...,
    "mawb": ...,
    "reqna": ...,
    "tms_number": ...,
    "weight_value": ...,
    "weight_unit": ...,
    "pieces": ...,
    "line_items": [...],
    "invoice_total": ...,
    "invoice_currency": ...
  }}
]

Document text:
"""
{extracted_text}
"""
