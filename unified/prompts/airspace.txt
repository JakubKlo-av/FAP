Return only the JSON. No explanations, no markdown.

OCR extract of an Airspace Technologies invoice.
Do not infer, calculate, or guess - extract exactly what appears in the OCR.

Document layout:
- TOP: invoice number (#XXXXXXX), invoice date
- LEFT: Bill To
- CENTER: Remit Payment To
- RIGHT: Total, Due Date
- ORDER TABLE: Terms | Due Date | Order Number | Order References | Commodity | Order Source | PO#
- TRANSPORT TABLE: Date | AWB | Tracking ID | Pickup Address | Delivery Address | Dims | POD
- ITEMS TABLE: Item | Description | Quantity | Amount

Fields to extract:

1. supplier - always: "Airspace"
2. invoice_no - top-right, line below "Invoice", starts with "#"; extract WITHOUT "#" (e.g. "3253904").
3. invoice_date - line below invoice_no; convert to DD/MM/YYYY.
4. bill_to - always: "Autoliv ASP, Inc. 3350 Airport Road Ogden, UT 84405 USA"
5. due_date - after "Due Date:" in the header; convert to DD/MM/YYYY.
6. order_number - ORDER TABLE, column "Order Number" (3rd col) until TRANSPORT TABLE header; collect all rows.
7. order_references - ORDER TABLE, column "Order References" (4th col) only; join with " " until TRANSPORT TABLE header. Do NOT read from Commodity column.
8. ship_date - TRANSPORT TABLE, column "Date" (1st col); convert to DD/MM/YYYY.
9. delivery_date - TRANSPORT TABLE, column "POD" (7th col); date format "Mon DD YY" -> DD/MM/YYYY.
10. delivery_time - TRANSPORT TABLE, "POD" column; 4 digits after date (e.g. "13 05" -> "13:05").
11. delivery_timezone - TRANSPORT TABLE, "POD" column; timezone abbreviation after time (e.g. "MST").
12. awb - TRANSPORT TABLE, "AWB" column (2nd col). If blank -> "N/A". Do NOT copy from tracking_id.
13. tracking_id - TRANSPORT TABLE, "Tracking ID" column (3rd col). OCR correction: "]" -> "J".
14. shipper - TRANSPORT TABLE, "Pickup Address" column (4th col) until ITEMS TABLE header; join with ", ".
15. consignee - TRANSPORT TABLE, "Delivery Address" column (5th col) until ITEMS TABLE header; join with ", ".
16. dims - TRANSPORT TABLE, "Dims" column (6th col) all rows joined with "; " until ITEMS TABLE header. If blank -> "N/A".
17. weight_value - sum of all weight numbers from all dims rows (numeric string, e.g. "285.0").
18. weight_unit - unit from dims column (e.g. "kg").
19. line_items - ITEMS TABLE rows until "Subtotal";
    for each row: item, description, quantity, amount (strip $ and commas).
    Return as JSON array ([] if none).
20. invoice_total - "Total" line (NOT Subtotal, NOT Amount Due); numeric only, strip $ and commas.
21. invoice_currency - currency from total line; "$" symbol -> "USD".

=== RULES ===
- Missing field -> "N/A" ([] for line_items).
- Never copy a value from another field if source is blank.
- weight_value is the only calculated field (sum of weights from dims rows).
- Extract only what is literally present in the OCR.
- Return a single JSON array with one object:

[
  {{
    "supplier": ...,
    "invoice_no": ...,
    "invoice_date": ...,
    "bill_to": ...,
    "due_date": ...,
    "order_number": ...,
    "order_references": ...,
    "ship_date": ...,
    "delivery_date": ...,
    "delivery_time": ...,
    "delivery_timezone": ...,
    "awb": ...,
    "tracking_id": ...,
    "shipper": ...,
    "consignee": ...,
    "dims": ...,
    "weight_value": ...,
    "weight_unit": ...,
    "line_items": [...],
    "invoice_total": ...,
    "invoice_currency": ...
  }}
]

Document text:
"""
{extracted_text}
"""
